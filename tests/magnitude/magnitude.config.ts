import { type MagnitudeConfig } from "magnitude-test";

export default {
    url: "http://localhost:5173",
    // headless: true, // Removed as it is not part of MagnitudeConfig
} satisfies MagnitudeConfig;
